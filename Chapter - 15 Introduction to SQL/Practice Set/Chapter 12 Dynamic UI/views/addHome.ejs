<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Home to Airbnb</title>
  <link href="/output.css" rel="stylesheet" />
</head>
<body class="bg-gray-50 min-h-screen">
  <!-- Include Header Partial -->
  <%- include('partials/header') %>

  <!-- Main Content -->
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="bg-white shadow-md rounded-lg p-8">
      <!-- Page Title -->
      <h1 class="text-red-700 text-3xl italic font-bold mb-8 text-center">Register your home here</h1>
      
      <!-- Form Container -->
      <div class="max-w-md mx-auto">
        <form action="/host/add-home" method="POST" class="space-y-6">
          <!-- House Name Input -->
          
            <input 
              type="text" 
              id="houseName"
              name="houseName" 
              placeholder="Enter the name of your house" 
              class="w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition-colors duration-200"
              required
            />
            <!-- Price -->
               <input 
              type="text" 
              id="price"
              name="price" 
              placeholder="Price Per Night" 
              class="w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition-colors duration-200"
              required
            />

            <!-- Location -->
                      <input 
              type="text" 
              id="location"
              name="location" 
              placeholder="Location" 
              class="w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition-colors duration-200"
              required
            />

            <!-- Rating -->
         
                           <input 
              type="text" 
              id="rating"
              name="rating" 
              placeholder="Rating" 
              class="w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition-colors duration-200"
              required
            />

            <!-- Photo -->
                              <input 
              type="text" 
              id="photoUrl"
              name="photoUrl" 
              placeholder="Enter Photo URL" 
              class="w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500 transition-colors duration-200"
              required
            />
          <!-- Submit Button -->
          <div class="text-center">
            <button 
              type="submit" 
              class="inline-flex items-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors duration-200 shadow-sm"
            >
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
              </svg>
              Submit
            </button>
          </div>
        </form>
      </div>
    </div>
  </main>
</body>
</html>